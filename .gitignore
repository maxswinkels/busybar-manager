node_modules/
logs/
apps/
# Build artifact: everyone builds the dashboard from web/src themselves
# (`npm run build` at the root, or scripts/install.sh). Keeping it out of git
# avoids a merge conflict on every frontend PR (see #14).
web/dist/
.venv/
__pycache__/
.DS_Store
config.json.tmp*
# Local runtime config: holds personal args (coords) and any library token —
# never commit. Ship config.example.json as the template instead.
config.json
config.json.corrupt-*
config.json.pre-cleanup-*
# Local Claude Code permission overrides
.claude/settings.local.json
